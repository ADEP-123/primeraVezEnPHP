<?php

/**
 * ? PHP Y MSQL
 * ? Modificadores o constraints
 * * AUTO_INCREMENT El valor se va incrementando automaticamente en cada registro, se aplica a enteros
 * * DEFAULT Coloca un valor por defecto 
 * * NOT_NULL impide que un campo sea nulo
 * * PRIMARY KEY hace que el camo se considere llave primaria
 * * UNIQUE 65565 bytes evita la repeticin de valores
 */
